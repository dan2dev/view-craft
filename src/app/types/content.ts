export type Feature = {
  title: string;
  description: string;
};

export type Example = {
  title: string;
  summary: string;
  code: string;
};

export type ApiItem = {
  name: string;
  signature: string;
  description: string;
  notes?: string;
};

export type Pattern = {
  title: string;
  description: string;
  code: string;
};
