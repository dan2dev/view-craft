{
  "name": "basic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "cd ../../ && pnpm run build && cd examples/basic && pnpm run build && vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "view-craft": "link:../.."
  }
}
